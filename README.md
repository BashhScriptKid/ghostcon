# ghostcon
A modern terminal system for UNIX TTY virtual terminals, matching modern terminal emulator functionality without additional complexity. Not a multiplexer; just TTYs that don't fall behind. 
